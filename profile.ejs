<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            margin: 0;
            padding: 0;
            background-color: #f8f9fa; 
            font-family: 'Arial', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }
        .form-container {
            background-color: #ffffff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
            width: 100%;
            max-width: 600px;
            text-align: center;
        }
        h1 {
            font-size: 28px;
            margin-bottom: 20px;
            color: #333;
        }
        .profile-info {
            margin-bottom: 20px;
            font-size: 16px;
            color: #555;
            text-align: left; 
            border-top: 1px solid #007bff;
            padding-top: 15px;
        }
        .profile-info p {
            margin: 10px 0;
            padding-bottom: 10px;
            border-bottom: 1px solid #007bff; 
        }
        .profile-info p:last-child {
            border-bottom: none; 
        }
        .profile-info strong {
            color: #333;
        }
        .profile-actions {
            display: flex;
            justify-content: space-between;
            margin-top: 30px; 
        }
        .profile-actions form,
        .profile-actions a {
            width: 48%;
        }
       .profile-actions button {
            width: 100%;
            padding: 12px;
            border-radius: 5px;
            border: none;
            font-size: 14px;
            cursor: pointer;
            font-weight: bold;
            color: #333;
            background-color: #ffc107; 
            transition: all 0.3s ease;
        }
        .profile-actions button:hover {
            background-color: #e0a800;
        }
        .logout {
            display: flex;
            justify-content: left;
            align-items: center;
            font-size: 16px;
            color: #007bff; 
            margin-top: 40px; 
        }
        .logout a {
            color: #007bff; 
            text-decoration: none;
            margin-left: 5px;
            font-weight: bold;
        }
        .logout a:hover {
            text-decoration: underline;
        }
        .logout i {
            margin-right: 5px;
        }
      .header {
          display: flex;
          justify-content: space-between;
          align-items: center;
          padding: 10px 20px;
          background-color: #333;
          position: fixed;
          top: 0;
          left: 0;
          right: 0;
          z-index: 1000;
          margin-bottom: 5px;
        }
        .header .logo span {
          color: white;
          font-size: 24px;
          font-weight: bold;
        }
        .header .nav-links {
          display: flex;
          gap: 20px;
        }
        .header .nav-links a {
          color: white;
          text-decoration: none;
          padding: 10px;
          font-size: 16px;
        }
        .header .nav-links a:hover {
          background-color: #444;
          border-radius: 4px;
        }
        body {
          margin-top: 60px;
        }

      .footer {
          background-color: #000; 
          color: white;           
          padding: 20px 0;       
          text-align: center;      
          position: fixed;
          bottom: 0;
          left: 0;
          right: 0;
          z-index: 1000;
        }
        
        .footer .footer-content p {
          margin: 0;  
          font-size: 14px;  
        }
        
    </style>
</head>
<body>
<header class="header">
    <div class="logo">
      <span>SnakeShield</span>
    </div>
    <div class="nav-links">
      <a href="/profile">Profile</a>
      <a href="/sos">SOS</a>
      <a href="/identify">Identify</a>
      <!-- <a href="/contacts">Contacts</a> -->
    </div>
  </header>
  
    <div class="form-container">
        <h1>Profile</h1>

        <!-- Profile Information -->
        <div class="profile-info">
            <p><strong>Username:</strong> <%= user.username %></p>
            <p><strong>Emergency Number:</strong> <%= user.emergencyNumber %></p>
            <p><strong>Gender:</strong> <%= user.gender %></p>
            <p><strong>Age:</strong> <%= user.age %></p>
        </div>

        <!-- Buttons for Edit and Delete actions -->
        <div class="profile-actions">
            <form action="/delete-profile" method="POST">
                <button type="submit" class="delete-btn"><i class="fas fa-trash"></i> Delete Account</button>
            </form>
            <a href="/edit-profile">
                <button class="edit-btn"><i class="fas fa-edit"></i> Edit Profile</button>
            </a>
        </div>

        <!-- Logout text -->
        <div class="logout">
            <i class="fas fa-sign-out-alt"></i>
            <a href="/logout">Logout</a>
        </div>
    </div>
    <!-- footer.html (partials page) -->
<footer class="footer">
    <div class="footer-content">
      <p>&copy; 2024 SnakeShield. All rights reserved.</p>
    </div>
  </footer>
  

</body>
</html>
